# Reinforcement Learning Trading (DQN)

Simple environment: hold/long/flat actions on a single asset; reward = daily return * position.
Run:
```
pip install -r ../requirements.txt
python main.py --ticker AAPL
```
